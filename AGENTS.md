# UI development

- Iterate on UI and visual design locally on macOS first.
- Apply the corresponding Windows changes only after the macOS design is settled.
- Keep intermediate visual experiments local instead of pushing each variant to CI.
